services:
  app:
    build:
      target: builder
    environment:
      - FLASK_ENV=development
    volumes:
      - ./app:/app  # hot reload
    command: ["python", "app.py"]
    ports:
      - "5000:5000"  # expose directly in dev
